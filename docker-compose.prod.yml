services:
  engine:
    volumes:
      - ./chroma_db:/app/chroma_db
      - ./data:/app/data
  api:
    environment:
      NODE_ENV: production
      ALLOW_MENTOR_REGISTRATION: "false"
      ALLOW_LOCAL_PROVIDER: "false"
